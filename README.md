# ITMO-453-Lab-2

This is a set of bash scripts that will automatically configure an ubuntu webserver VM. Secondarily, it also includes an ansible playbook and role setup, which allows you to easily run all the configuration with one command. This can be done easily by cloning this repository to a new ubuntu webserver VM